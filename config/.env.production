# Production environment variables
NODE_ENV=production

# Server configuration
PORT=3000
HOST=127.0.0.1

# Frontend URL for CORS (update with your VPS domain/IP)
FRONTEND_URL=http://e-aramaica.ru

# API URL (for client to connect to server)
VITE_API_URL=/api