# Local production environment (testing production setup locally)
NODE_ENV=production

# Server configuration
PORT=3000
HOST=127.0.0.1

# Frontend URL for CORS (localhost for local testing)
FRONTEND_URL=http://localhost

# API URL (for client to connect to server via nginx proxy)
VITE_API_URL=/api